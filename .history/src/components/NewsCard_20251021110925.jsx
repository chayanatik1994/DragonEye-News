import React from 'react';

const NewsCard = ({news}) => {
    return (
        <div>
            {news.title}
            <img src={img.news.url} alt="/>
        </div>
    );
};

export default NewsCard;

